import React from 'react';
import ScrollingBar from './ScrollingBar';
import './Home.css';


const Home = () => {
      return (
            <div className="home-page">
            <ScrollingBar />
            </div>
      );
};

export default Home;
